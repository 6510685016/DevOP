# Environment: DEV
# Application: frontend-app-a

replicaCount: 1

image:
  tag: "latest"

ingress:
  enabled: True
  host: frontend-app-a.dev.company.com
  annotations:
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 50m
    memory: 64Mi
